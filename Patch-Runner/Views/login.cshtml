@inherits Nancy.ViewEngines.Razor.NancyRazorViewBase<dynamic>

    @{
    Layout = "_Master.cshtml";
    }
    <div class="container">
        <div class="row">
            <div class="col-md-8 well">
                <h3 class="legend">Patch Runner</h3>
                @if(ViewBag.HasError)
                {
                <div id="signin-fail" class="alert alert-error">@ViewBag.ErrorMessage</div>
                }
                <form method="post" role="form">
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="text" name="username" class="form-control" placeholder="Username" value="biofractal">
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" name="password" class="form-control" placeholder="Password" value="G*w56qY%-8sA">
                    </div>
                    <div class="pull-right">
                        <input type="submit" class="btn btn-info" value="Sign in" />
                    </div>
                </form>
            </div>
        </div>
    </div>
